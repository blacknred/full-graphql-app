overwrite: true
schema: "http://localhost:8080"
documents: "src/lib/graphql/**/**.graphql"
generates:
  src/types/index.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
 