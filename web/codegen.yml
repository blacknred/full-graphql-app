overwrite: true
schema: "http://localhost:8080"
documents: "src/graphql/**/**.graphql"
generates:
  src/typings.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
 